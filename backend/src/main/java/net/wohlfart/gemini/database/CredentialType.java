package net.wohlfart.gemini.database;


/**
 * stateless strategy to perform an authentication action for a user
 * - passwd validation
 * - oauth redirect
 */
public enum CredentialType {
  LOCAL_PASSWD,

  ;
}
